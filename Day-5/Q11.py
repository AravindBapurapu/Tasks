# In Python 3, what is the output of type(range(5)). (What data type it will
# return)

# Ans : it will return range type, because we are using the range which is predefined function in python. so if you try to print 
#  this value then it will returns range type.